# 参与贡献指南

感谢你愿意参与 CodeDaoLab（码上飞升）的开发。请先阅读本指南，保持协作高效有序。

## 行为准则

- 友好、专业、尊重他人；不接受人身攻击与歧视性言论。
- 讨论聚焦技术方案与产品目标，避免情绪化争论。

## 提交 Issue

- 先搜索已有 Issue，避免重复。
- 使用清晰的标题，格式建议：`[类型] 简述`，类型如 `Bug` / `Feature` / `Question` / `Docs`。
- Bug 报告请包含：
  - 运行环境（操作系统 / Python 版本 / 运行方式）
  - 复现步骤（尽量简短）
  - 实际行为与预期行为
  - 相关日志或截图（如有）
- 功能建议请说明：动机、期望行为、对现有功能的影响。

## Pull Request 流程

1. Fork 本仓库，从 `main` 创建功能分支：`feat/xxx`、`fix/xxx`、`docs/xxx`。
2. 小步提交，提交信息遵循 [Conventional Commits](https://www.conventionalcommits.org/) 风格（如 `feat: ...` / `fix: ...` / `docs: ...`）。
3. 变更前先跑通测试：
   ```bash
   python -m pytest -q
   ```
   涉及前端时运行 `node scripts/e2e_test.js`。
4. 提交 PR 时描述：变更内容、动机、测试结果、截图（UI 变更时）。
5. 保持 PR 小而聚焦；一个 PR 解决一个问题。

## 代码规范

- 后端：Python 3.10+；遵循 PEP 8；关键逻辑保留中文注释（项目内统一风格）。
- 前端：原生 JavaScript（无构建链）；保持现有命名与结构，不引入新框架。
- 数据：所有游戏内容优先 JSON 数据驱动，禁止硬编码新任务/剧情/NPC。
- 沙箱安全：任何涉及玩家代码执行的改动必须通过 `tests/test_sandbox.py` 全量用例，禁止削弱隔离限制。
- 禁止引入密钥、Token 与用户数据到仓库。

## 文档规范

- 用户可见文案使用中文；代码标识符使用英文。
- 新增功能必须同步更新 `DEVLOG.md` 与对应 `docs/` 文档。
- README 只描述已实现功能，不虚构规划中的能力。
