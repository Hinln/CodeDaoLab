# 《码上飞升》V0.3 测试报告

- 测试日期：2026-08-08
- 测试版本：v0.3.0
- 总体结果：通过

## JavaScript 语法检查

命令：

```powershell
node --check static/js/app.js
```

结果：通过，无语法错误。

## Python 自动化测试

命令：

```powershell
python -m pytest -q
```

结果：

```text
268 passed, 32 skipped in 71.15s
```

覆盖范围包括世界移动与解锁、NPC 关系、功法成长、剧情任务、奇遇、长期成长聚合，以及既有主线、沙箱、存档、秘境、成就和渡劫功能。

## 浏览器端到端测试

命令：

```powershell
node scripts/e2e_test.js
```

结果：`ALL E2E CHECKS PASSED`

验证链路包括角色创建、14 地点世界移动、地点功能入口、NPC 对话、主线提交、突破、藏经阁、演武场功法考核、任务快进、随机奇遇、秘境首通、炼丹房、渡劫台、筑基、金丹和 V0.3 存档恢复。

## 发行构建与冒烟测试

命令：

```powershell
python scripts/build.py
```

结果：9 / 9 通过。

```text
[PASS] 首页渲染
[PASS] 静态资源服务
[PASS] 编辑器资源服务
[PASS] 无存档初始状态
[PASS] 创建角色
[PASS] 沙箱真实执行
[PASS] 提交验证通过
[PASS] 课程数据从归档加载
[PASS] 秘境数据从归档加载
```

生成产物：

- `dist/码上飞升-v0.3.0.pyz`（172 KB）
- `dist/启动游戏.bat`

## 测试结论

V0.3 的单元/集成测试、浏览器完整流程和发行包冒烟测试均通过，未发现阻断发布的问题。
